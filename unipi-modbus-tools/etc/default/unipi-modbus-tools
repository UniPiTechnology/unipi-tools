# Defaults for unipi-modbus-tools initscript
# sourced by /etc/init.d/unipi-modbus-tools
# installed at /etc/default/unipi-modbus-tools by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
# Parameter -a 255 is required for Codesys. 
# Common access is available via Modbus address 255 (and also 0)
DAEMON_OPTS="-a 255"
